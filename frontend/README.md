# Subtrack Frontend

개인 구독 관리 대시보드의 React 19 + Vite 프런트엔드입니다.

```bash
npm ci
npm run dev
```

기본 API 주소는 `http://localhost:8080/api`이며, 배포 환경에서는 `VITE_API_URL`로 변경할 수 있습니다.

```bash
npm run lint
npm run build
```
