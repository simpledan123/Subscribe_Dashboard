import {useState} from 'react';import api from '../api'
export default function Login({onLogin}){const [form,setForm]=useState({username:'demo',password:'demo1234'}),[register,setRegister]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{if(register)await api.post('/auth/register',form);const r=await api.post('/auth/login',form);localStorage.setItem('token',r.data.token);onLogin()}catch(err){setError(err.response?.data?.error||err.response?.data?.message||'로그인하지 못했습니다.')}finally{setBusy(false)}};return <div className="login-container"><div className="login-box"><div className="brand" style={{justifyContent:'center',color:'#172033',paddingBottom:18}}><span className="brand-mark" style={{color:'#fff'}}>S</span><div><strong>Subtrack</strong><small>Personal subscriptions</small></div></div><h2>{register?'나만의 공간 만들기':'다시 오셨네요'}</h2><p className="login-subtitle">흩어진 계정, 결제일, 무료 혜택을 한눈에</p>{!register&&<div className="hint">데모 프로필: demo / demo1234</div>}{error&&<div className="error-banner">{error}</div>}<form onSubmit={submit} data-testid="login-form"><input data-testid="login-username" value={form.username} onChange={e=>setForm({...form,username:e.target.value})} placeholder="아이디" required/><input data-testid="login-password" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="비밀번호" required/><button data-testid="login-submit" disabled={busy}>{busy?'처리 중...':register?'가입하고 시작하기':'로그인'}</button></form><p className="toggle-text">{register?'이미 계정이 있나요?':'처음이신가요?'} <span data-testid="login-toggle" onClick={()=>setRegister(!register)}>{register?'로그인':'회원가입'}</span></p></div></div>}
